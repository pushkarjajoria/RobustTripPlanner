name: "base"
channels:
  - defaults
dependencies:
  - numpy
  - pandas
  - matplotlib
  - ipywidgets
  - plotly=4.14.3
  - hdfs3
  - pyarrow=4.0.0
  - voila=0.2.7
  - pip
  - pip:
      - scikit-learn
      - geopandas
# - add packages here
# - one per line
prefix: "/opt/conda"

